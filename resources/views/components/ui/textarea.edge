@let(classes = [
    `mt-1 block w-full rounded-md border
    border-slate-300 px-3 py-2 text-sm`
])

<textarea {{ $props
    .except(['value'])
    .merge({ class: classes })
    .toAttrs() }}
>
    {{{ $props.get('value') || '' }}}
</textarea>